<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "What's happening?" %>
  </div>
  <%= f.submit "Tweet", class: "btn btn-primary" %>
<% end %>