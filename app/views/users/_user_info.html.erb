<%= link_to gravatar_for(current_user), show_path %>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", show_path %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>